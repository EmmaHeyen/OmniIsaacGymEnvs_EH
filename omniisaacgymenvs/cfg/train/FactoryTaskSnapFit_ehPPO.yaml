params:
  seed: ${...seed}
  algo:
    name: a2c_continuous

  model:
    name: continuous_a2c_logstd

  network:
    name: actor_critic
    separate: False

    space:
      continuous:
        mu_activation: None
        sigma_activation: None
        mu_init:
          name: default
        sigma_init:
          name: const_initializer
          val: 0
        fixed_sigma: True
    mlp:
      units: [256, 128, 64]
      activation: elu
      d2rl: False

      initializer:
        name: default
      regularizer:
        name: None

  load_checkpoint: ${if:${...checkpoint},True,False}
  load_path: ${...checkpoint}

  config:
    name: ${resolve_default:FactoryTaskSnapFit_eh,${....experiment}}
    full_experiment_name:     30_FactoryTaskSnapFit_eh_numEnvs128_numEpochs1000_stiffness0.01_damping0_keypointP_controllerB_frankaNoiseTrue_MaleNoiseFalse_FemaleNoiseFalse #${.name}
    device: ${....rl_device}
    device_name: ${....rl_device}
    env_name: rlgpu
    multi_gpu: False
    ppo: True
    mixed_precision: True
    normalize_input: True
    normalize_value: True
    value_bootstrap: True
    num_actors: ${....task.env.numEnvs}
    reward_shaper:
      scale_value: 1.0
    normalize_advantage: True
    gamma: 0.99
    tau: 0.95
    learning_rate: 1e-4
    lr_schedule: fixed
    schedule_type: standard
    kl_threshold: 0.016
    score_to_win: 20000
    max_epochs: 1000 #${resolve_default:400,${....max_iterations}} # default:400
    save_best_after: 50 
    save_frequency: 25
    print_stats: True
    grad_norm: 1.0
    entropy_coef: 0.003 # laut medium blog: entropy coefficient range from 0.001 to 0.01
    truncate_grads: False
    e_clip: 0.2
    horizon_length: 128
    minibatch_size: 512
    mini_epochs: 8
    critic_coef: 2
    clip_value: True
    seq_length: 4
    bounds_loss_coef: 0.0001
